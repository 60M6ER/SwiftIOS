Чтобы наша коллекция фильмов отображалась корректно, нам нужно сделать несколько важных шагов. Итак, наша тактика в этом юните, дорогие друзья, будет следующей:

Добавим UICollectionView на ViewController:
привяжем UICollectionView к пустому контроллеру;
добавим элемент для поиска;
расширим наш класс MainViewController;
Соберём кастомную ячейку для UICollectionView:
соберём ячейку в сториборде;
сделаем кастомный класс для ячейки;
опишем в нём модель ячейки;
интегрируем кастомную ячейку в имеющийся код.
Посмотрим, что у нас получается, и как мы будем с этим работать
UICollectionView + UIViewController
Продолжаем работать с проектом из первого юнита.

Связь для CollectionView:
зажимаем на клавиатуре ctrl, нажимаем левой кнопкой мыши по CollectionView на контроллере — появится синяя линия
ведём линию в самый верх контроллера — в первый значок
вот так:


появится специальное выпадающее меню, в котором надо выбрать dataSource и delegate. При этом выбрать один пункт можно один раз. Так что придётся эту операцию повторить. Нам надо, чтобы стало так:

Работа с кодом MainViewController
Да, этот момент настал, сейчас мы будем реализовывать довольно важные вещи! Переходим в MainViewController.swift:
-->
Необходимые аутлеты
Сейчас мы свяжем с классом MainViewController наши CollectionView и SearchBar. Наверно тут нет смысла расписывать, как это делается, мы просто покажем результат кода. Тем временем, мы в вас верим — вы справитесь!

Тадам! В нашем классе MainViewController приросло два аутлета:


Связь для CollectionView и SearchBar через код
Помните, мы говорили про обеспечение связи для двух элементов с контроллером в коде, а без визуальной привязки? Если вы уже сделали привязку через сториборд, то можете её отменить.

Давайте сейчас это сделаем в методе viewDidLoad() нашего MainViewController:


И сразу ошибки… В голову приходят весьма знакомые мысли, слова негодования озвучиваются уже как-то сами по себе.

Расширяем MainViewController
Делаем extension под классом MainViewController и прописываем ему три протокола — UICollectionViewDataSource, UICollectionViewDelegate и UISearchBarDelegate:


У нас снова вылезла ошибка — решить её можно, добавив методы вручную, либо нажать Fix. Вот что нам в итоге надо — знакомые по TableView методы numberOfItemsInSection и cellForItemAt indexPath:


Кстати, если вы делали привязку элементов к контроллеру через код, то после добавления протоколов ошибки исчезнут:


Вы знаете, что мы сделали? Мы расширили функционал нашего MainViewController и дали ему возможность реализовать отрисовку ячеек в зависимости от количества элементов в источнике данных.

Нам не нужно искать ячейку в библиотеке UIKit, так как у нас уже есть ячейка «из коробки»:


Нашей ячейке нужен Reuse Identifier. Выбираем ячейку и идём в Attributes Inspector и задаём его:

-->
У Reuse Identifier нашей ячейки значение будет FilmCell

Мы хотим сделать что-то типа этого: filmCell.png

Однако в точности так же мы делать не будем, потому что здравый смысл подсказывает сделать отображение в два ряда на экране + все текстовые лейблы убрать вниз под постер.

Кстати, не забудьте у лейблов сделать безлимитное количество строк, то есть 0 у всех трёх:

Модель ячейки
Раз уж мы собрали визуальную версию ячейки, давайте завершим начатое — соберём модель ячейки в коде со всеми аутлетами. Это нужно, чтобы мы пользовались собственной ячейкой и видели не дефолтные вещи из коробки, а типа этого:


Давайте создадим UICollectionViewCell-класс через Cocoa Touch классы. Для этого выбираем File -> New -> File… :


Выбираем Cocoa Touch Class и нажимаем Next:


Даём файлу любое название, у нас это будет FilmCollectionViewCell по логике, пока не трогаем XIB, просто нажимаем Next:


Сохраняем файл в папке с проектом и приступаем к работе с ним:


У ячейки должен быть свой кастомный класс. Поэтому вернёмся на сториборд, выберем ячейку и в Identity Inspector добавим наш FilmCollectionViewCell:

  -->
В класс нам пока надо добавить только аутлеты нашего Image View и лейблов:


Модель ячейки готова! Теперь вернёмся в MainViewController, в метод cellForItemAtIndexPath. В нём мы создадим ссылку на ячейку, чтобы метод мог отрисовывать её при обращении:


Заметьте — мы развернули опционал, потому typecasting мы делали через as?, а не через as! -> так безопаснее.

В итоге мы получим такой результат:


И теперь у нас есть доступ ко всем элементам ячейки через экземпляр cell. Например, к posterPreviewImageView:

Подошёл наш плановый тест — сейчас мы увидим, всё ли мы сделали правильно. Для этого перейдём в JSONModel.swift и под классом JSONModel создадим класс для тестовых объектов. Обращаем ваше внимание, что этот класс нам нужен буквально на итерацию теста, и в дальнейшем мы никак не будем его использовать: CodeLesson2.1.png

Пусть в нашей коллекции будет 10-15 элементов. Для этого загрузите в папку Assets картинки из архива ниже:

Папка "Архив_с_изображениями_второй_проектный_юнит"
Все изображения представляют собой скриншоты изображений с сайта Unsplash.com

CodeLesson2.2.png

Теперь с нашим массивом типа [TestModel] идём в numberOfItemsInSection и прописываем строчку, которая будет нам возвращать число ячеек соответственно числу элементов в массиве -> подсчёт будет осуществляться через count:


Финальный шаг — связать ячейку с данными из массива:

в Image View отправляется изображение из массива;
в лейблы — соответствующие значения.

В случае с изображением мы можем позволить себе nil coalescing, чтобы не создавать сложные цепочки ради теста. Но здесь можно сделать заглушку в виде специальной картинки. Можете добавить ещё сами, всё так же — через Assets.
Запускаем приложение на симуляторе и видим, что наш тест весьма успешный:

Помните, мы видели красивую ячейку в приложении Кинопоиск? Вот она:


Почему красивая — потому что соотношение отступов между элементами весьма приятное глазу. Давайте самостоятельно сделаем так, чтобы наша ячейка хоть немного была похожа на «эталон», то есть ячейку из приложения Кинопоиск.

Вы можете пользоваться любыми инструментами — использовать StackView или полностью отказаться от него, делать Constraints так, как будет удобнее в плане результата. Главное, чтобы текстовые элементы в ячейке занимали меньше 30% самой ячейки.

Вот та ячейка, что у нас сейчас есть:


А вот, что мы можем получить в итоге:

 или это
Есть важный хинт — это задание не имеет правильного ответа. Все дальнейшие действия зависят исключительно от ваших ощущений и желаний видеть плитку с фильмами так или иначе.

Поэтому у нас есть небольшие критерии для этого задания, но нет строгого гайда, по которому мы будем оценивать ваши результаты.

Нужно собирать приложение с этими условиями. вот критерии оценки будут тут: КритерииОценкиПо2Юниту.png
